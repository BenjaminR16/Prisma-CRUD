generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("SUPABASE_URL")
}

model productos {
  nombre      String
  descripcion String
  precio      Float
  id          Int     @id @default(autoincrement())
  imagen      String?
}
